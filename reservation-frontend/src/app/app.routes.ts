import { Routes } from '@angular/router';
import { ReservationsPageComponent } from './component/reservations-page/reservations-page.component';

export const routes: Routes = [
	{
		path: '',
		component: ReservationsPageComponent
	}
];
